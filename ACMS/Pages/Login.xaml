<Page x:Class="ACMS.Pages.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ACMS.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Login">
    <Page.Resources>

        <Style x:Key="LeftMenu" TargetType="Border">
            <Setter Property="Background" Value="#8c7851"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#7A6947"/>
            <Setter Property="CornerRadius" Value="20"/>
        </Style>

        <Style x:Key="SelectedLeftMenu" TargetType="Border">
            <Setter Property="Background" Value="#f9f4ef"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#ECE6E0"/>
            <Setter Property="CornerRadius" Value="20"/>
        </Style>

        <Style x:Key="AllText" TargetType="Label">
            <Setter Property="FontSize" Value="20"/>
        </Style>
    </Page.Resources>

    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition Height="25"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource LeftMenu}"></Border>
        <Border Grid.Row="1" Grid.Column="2" Style="{StaticResource LeftMenu}"></Border>
        <Border Grid.Row="1" Grid.Column="3" Style="{StaticResource LeftMenu}"></Border>
        <Border Grid.Row="1" Grid.Column="4" Style="{StaticResource LeftMenu}"></Border>
        <Border Grid.Row="1" Grid.Column="5" Style="{StaticResource LeftMenu}"></Border>
        <Border Grid.Row="1" Grid.Column="6" Style="{StaticResource LeftMenu}"></Border>
        <StackPanel Grid.ColumnSpan="9" Grid.Row="3" Background="#f9f4ef">

            <Label Content="Начальная страница" FontSize="20" HorizontalAlignment="Center" Margin="0 10 0 0" FontWeight="Bold"/>
        </StackPanel>
        <Border Grid.Column="0" Grid.Row="1" Style="{StaticResource SelectedLeftMenu}"/>

    </Grid>
</Page>
