<Page x:Class="ACMS.Pages.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ACMS.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      TextElement.Foreground="{DynamicResource MaterialDesignBody}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Login">
    <Page.Resources>

        <Style x:Key="ButtSwap" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="30 10 0 0"/>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Gray"/>
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="0 0 0 2"/>
                </Trigger>
            </Style.Triggers>
            
        </Style>
    </Page.Resources>

    <Grid Background="Transparent" >
        <Border CornerRadius="10 0 0 10" x:Name="LoginGrid" Background="White" Width="350" HorizontalAlignment="Left">
            <StackPanel>
                <WrapPanel Height="50">
                    <Button Content="Вход" Style="{StaticResource ButtSwap}"/>
                    <Button Content="Регистрация" Style="{StaticResource ButtSwap}"/>
                </WrapPanel>

                <Label Content="Авторизация"/>

                <TextBox />
                <PasswordBox/>

                <Button x:Name="AuthButt" Content="Войти"/>

            </StackPanel>

        </Border>        


    </Grid>
</Page>
